# Phase 3 screenshots

Capture list and filenames are in `docs/DEPLOYMENT.md` section 6.

Two rules that matter more than the rest:

- Capture at a normal window size, not maximised on a 4K panel. A screenshot
  nobody can read at submission width proves nothing.
- Never crop out a URL bar or a timestamp. They are what makes the screenshot
  evidence rather than decoration.
