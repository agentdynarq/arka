{
  "name": "@arka/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas and inferred types for the boundary between the gateway and every service. Frozen after 27 July, see ../../docs/adr.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^7.0.0"
  }
}
