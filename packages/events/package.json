{
  "name": "@arka/events",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Outbox and Redis Streams event bus primitives, reusable by any service. Same transaction as the state change, at-least-once delivery, dedupe by event id.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo 'no build step: consumed as source via workspace TS paths'",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured yet'"
  },
  "dependencies": {
    "pg": "^8.22.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.20.0",
    "typescript": "^7.0.0"
  }
}
