{
  "name": "@arka/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared design tokens and components for apps/web and apps/console. Every color, radius and spacing value lives in src/tokens.css; components read tokens, never hardcode a value, so a later visual overhaul is a tokens edit, not a rewrite. See src/README.md.",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css",
    "./components.css": "./src/components.css"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo 'no build step: consumed as source via workspace TS paths'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no unit tests: this package is presentational, covered by the apps that render it'",
    "lint": "echo 'no lint configured yet'"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "typescript": "^5.7.3"
  }
}
