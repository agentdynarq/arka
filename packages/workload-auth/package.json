{
  "name": "@arka/workload-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Short-lived, HMAC-signed workload identities services present to each other. Zero runtime dependencies, same rationale as ledger-core.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^7.0.0"
  }
}
