{
  "name": "@arka/ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ledger service for one Cell. Framework-free core over @arka/ledger-core; the HTTP layer is a thin adapter.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo 'no build step: consumed as source via workspace TS paths'",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured yet'"
  },
  "dependencies": {
    "@arka/ledger-core": "workspace:*",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.20.0",
    "typescript": "^7.0.0"
  }
}
